# Архитектура

[← К оглавлению](./README.md) · Связанные: [data-model.md](./data-model.md) · [stage-1.md](./stage-1.md) · [stage-2.md](./stage-2.md)

## Содержание

- [Принципы](#принципы)
- [Слоистая архитектура](#слоистая-архитектура)
- [Диаграмма компонентов](#диаграмма-компонентов)
- [Поток создания ссылки](#поток-создания-ссылки)
- [Поток редиректа — Этап 1 (синхронный)](#поток-редиректа--этап-1-синхронный)
- [Поток редиректа — Этап 2 (асинхронный)](#поток-редиректа--этап-2-асинхронный)
- [Async click pipeline](#async-click-pipeline)
- [Анализ backpressure](#анализ-backpressure)
- [Graceful shutdown и drain](#graceful-shutdown-и-drain)
- [Kafka: семантика доставки](#kafka-семантика-доставки)
- [Решение 301 vs 302](#решение-301-vs-302)

## Принципы

1. **Строгое разделение слоёв.** `handler` знает про HTTP, `service` — про бизнес-правила,
   `repository` — про БД. Зависимости направлены внутрь: handler → service → repository.
   Service ничего не знает про `http.Request`, repository ничего не знает про бизнес-логику.
2. **Интерфейсы на границах.** Service зависит от интерфейса репозитория, а не от конкретной
   реализации на pgx. Это даёт подмену в тестах и развязку.
3. **`context.Context` пробрасывается везде** — от handler до запроса в БД. Дедлайны и отмена
   работают сквозь все слои.
4. **Идиоматичные ошибки.** Доменные ошибки-сентинелы (`ErrNotFound`, `ErrExpired`,
   `ErrAliasTaken`), обёртка через `%w`, разбор через `errors.Is/As`. HTTP-коды выставляет
   только handler, мапя доменные ошибки на статусы.
5. **Этап 1 намеренно наивен.** Синхронный инкремент счётчика — это не баг, это учебная
   точка боли. Не оптимизируем её до Этапа 2.

## Слоистая архитектура

```
┌──────────────────────────────────────────────────────────────┐
│  transport / handler  (internal/transport/http)              │
│  - парсит/валидирует HTTP-запрос                              │
│  - вызывает service                                          │
│  - мапит доменные ошибки → HTTP-коды                         │
│  - НЕ знает про SQL                                          │
└───────────────────────────┬──────────────────────────────────┘
                            │ доменные типы, context
┌───────────────────────────▼──────────────────────────────────┐
│  service  (internal/service)                                 │
│  - бизнес-правила: генерация кода, проверка TTL,             │
│    идемпотентность, валидация алиаса                         │
│  - оркестрирует repository + cache (+ канал кликов на Эт.2)  │
│  - НЕ знает про http.Request и про конкретный драйвер БД     │
└───────────────────────────┬──────────────────────────────────┘
                            │ интерфейсы Repository, Cache
┌───────────────────────────▼──────────────────────────────────┐
│  repository  (internal/repository/postgres, .../redis)       │
│  - SQL через pgxpool, маппинг строк в доменные структуры     │
│  - НЕ содержит бизнес-логики                                 │
└──────────────────────────────────────────────────────────────┘
```

Интерфейсы объявляются **на стороне потребителя** (в пакете `service`), а не в пакете
репозитория — это идиоматично для Go и не даёт репозиторию диктовать контракт.

```go
// internal/service/link.go
type LinkRepository interface {
    Create(ctx context.Context, l Link) (Link, error)
    GetByCode(ctx context.Context, code string) (Link, error)
    GetByURL(ctx context.Context, url string) (Link, error) // для идемпотентности
    IncrementClicks(ctx context.Context, code string) error // Этап 1
    Delete(ctx context.Context, code string) error
}

type Cache interface {
    Get(ctx context.Context, code string) (Link, bool, error)
    Set(ctx context.Context, l Link, ttl time.Duration) error
    Del(ctx context.Context, code string) error
}
```

## Диаграмма компонентов

### Этап 1

```
                ┌──────────┐
   client ─────▶│   chi    │  /api/v1/links, /{code}, /healthz, /readyz
                │  router  │
                └────┬─────┘
                     │
              ┌──────▼───────┐
              │  LinkService │
              └───┬──────┬───┘
                  │      │
          ┌───────▼─┐  ┌─▼────────┐
          │  Redis  │  │ Postgres │
          │ (cache) │  │ (pgxpool)│
          └─────────┘  └──────────┘
```

### Этап 2 (добавления выделены)

```
                ┌──────────┐
   client ─────▶│   chi    │──▶ [rate-limit middleware: token bucket]
                │  router  │     /metrics  (Prometheus)
                └────┬─────┘
                     │
              ┌──────▼───────┐
              │  LinkService │───────────────┐
              └───┬──────┬───┘               │ click event
                  │      │                   ▼
          ┌───────▼─┐  ┌─▼────────┐   ┌──────────────┐
          │  Redis  │  │ Postgres │◀──│ buffered chan │
          └─────────┘  └────▲─────┘   └──────┬───────┘
                            │ batch          │
                       ┌────┴──────────┐     │
                       │  worker pool  │◀────┘
                       │ (batch flush) │
                       └───────────────┘
                            │ fire-and-forget
                       ┌────▼──────┐      ┌───────────────┐
                       │  Kafka    │      │ expiry sweeper │ time.Ticker
                       │  producer │      │  (goroutine)   │
                       └───────────┘      └───────────────┘
                          topic: clicks
```

## Поток создания ссылки

Одинаков для обоих этапов.

```
POST /api/v1/links {url, custom_alias?, ttl_seconds?}
        │
   handler: декод JSON, валидация (url не пуст, схема http/https,
            алиас по [a-zA-Z0-9_-]{3,32}, ttl_seconds >= 0)
        │
   service:
     1. нормализовать URL
     2. проверить custom_alias по reserved-списку (metrics/api/... → 400)
     3. если custom_alias задан → code = alias, is_custom = true
        иначе → сгенерировать code (base62 от счётчика/хэша)
     4. вычислить expires_at = now + ttl (если ttl задан)
     5. repo.Create(...)  ── полагается на констрейнты:
          - PK(code)                         → коллизия алиаса = ErrAliasTaken (409)
          - partial UNIQUE(url_hash)         → идемпотентность ТОЛЬКО для «простых»
            WHERE is_custom=false              ссылок (без алиаса и TTL): тот же URL → тот же код
              AND expires_at IS NULL
        │
   repo: для простой ссылки — INSERT ... ON CONFLICT (партиальный индекс) DO NOTHING
         RETURNING ...; если строки нет → SELECT существующей (тот же код).
         для алиаса/TTL — обычный INSERT, дедуп по URL не применяется.
        │
   handler: 201 Created {code, short_url, expires_at}
            (или 200, если вернулась существующая простая ссылка — см. api.md)
```

Идемпотентность реализуется на уровне БД через **частичный** `UNIQUE` по хэшу URL (только для
ссылок без алиаса и без TTL) — это надёжно при гонке двух одновременных вставок одного URL и
при этом не ломает кастомные алиасы и срочные ссылки. Правило приоритета и обоснование —
в [data-model.md](./data-model.md#идемпотентность-создания).

## Поток редиректа — Этап 1 (синхронный)

Счётчик инкрементится **на горячем пути, блокируя ответ**. Это намеренно.

```
GET /{code}
   │
   ├─▶ Redis GET code ──── hit ──┐
   │        miss                 │
   │         │                   │
   │   Postgres SELECT by code   │
   │         │                   │
   │   not found → 404           │
   │         │                   │
   │   Redis SET code (populate) │
   │         ▼                   │
   ├──────── проверка expires_at ◀┘
   │         │ просрочена → 410 Gone
   │         ▼
   │   Postgres UPDATE click_count = click_count + 1   ◀── СИНХРОННО, БЛОКИРУЕТ
   │         │
   │         ▼
   └─▶ 302 Found, Location: original_url
```

Узкое место: каждый редирект ждёт `UPDATE` в Postgres. Под нагрузкой p99 редиректа
тащит за собой латентность записи в БД и контеншн по строке счётчика. Именно это
мы измеряем и потом убираем.

## Поток редиректа — Этап 2 (асинхронный)

```
GET /{code}
   │
   ├─▶ Redis GET code ── hit ──┐
   │        miss               │
   │   Postgres SELECT         │
   │   Redis SET (populate)    │
   │         ▼                 │
   ├──────── проверка TTL ◀─────┘
   │         │ просрочена → 410
   │         ▼
   │   clickCh <- ClickEvent{code, ts}   ◀── НЕ блокирует (буфер + drop-политика)
   │         │  (если буфер полон → drop + метрика, ответ не задерживаем)
   │         ▼
   └─▶ 302 Found, Location: original_url   ◀── отвечаем СРАЗУ
```

Ответ больше не зависит от записи в БД. Запись делает воркер-пул асинхронно. Это и есть
рефакторинг, ради которого затевался проект.

## Async click pipeline

Сердце Этапа 2. Один буферизованный канал → пул воркеров → батчевый флаш.

```
   redirect handlers (N горутин)
        │  clickCh <- event   (неблокирующий send, см. backpressure)
        ▼
 ┌────────────────────────────┐
 │  clickCh: chan ClickEvent  │  буфер = CLICK_CHANNEL_BUFFER (напр. 10000)
 └──────────────┬─────────────┘
                │ range clickCh
   ┌────────────┼────────────┬────────────┐
   ▼            ▼            ▼            ▼
 worker0      worker1      worker2     workerK     (WORKER_POOL_SIZE воркеров)
   │
   │  каждый воркер накапливает локальный батч map[code]int
   │  + счётчик СОБЫТИЙ pending (не len(map)!)
   │  flush когда:  pending >= BATCH_SIZE_N    (по числу событий)
   │            ИЛИ  сработал time.Ticker(FLUSH_INTERVAL_T)  (по времени)
   │            ИЛИ  канал закрыт (drain на shutdown)
   ▼
 Postgres: один UPDATE ... FROM (VALUES ...) — агрегированный апдейт
           click_count += delta для всех кодов батча за один запрос
   │
   └─▶ (опционально) Kafka producer: каждое событие → topic clicks (fire-and-forget)
```

Ключевые свойства:

- **Агрегация в батче по `code`.** Если один код кликнули 50 раз за окно, это `+50` одним
  обновлением строки, а не 50 апдейтов. Резко снижает контеншн и число round-trip.
- **Триггер размера считает СОБЫТИЯ, а не коды.** Батч — это `map[code]int`, поэтому
  `len(batch)` = число *уникальных кодов*, а не накопленных кликов. На редирект-трафике
  с перекосом ключей (несколько популярных кодов) `len(batch)` почти не растёт, и
  `len(batch) >= N` **не сработает никогда** — флаш повиснет только на таймере. Поэтому
  ведём отдельный счётчик событий `pending` и сравниваем с `N` именно его. Метрика
  `urlshort_click_batch_size` тоже наблюдает `pending` (события во флаше), а не `len(map)`.
- **Два триггера флаша.** Размер `N` (по событиям) ловит всплески трафика, таймер `T`
  гарантирует, что при низком трафике счётчики не «зависают» надолго невидимыми. Что раньше.
- **Идемпотентность не нужна** — это инкремент-дельты, не set. Но при сбое флаша батч можно
  потерять; для учебного счётчика это допустимо (см. backpressure и Kafka).

Псевдокод воркера. Обрати внимание на **два контекста**: `appCtx` живёт всё время работы
воркера (используется для записи в БД), а сигнал завершения приходит через **закрытие
канала**, а не через отмену `appCtx` — это и делает дренаж по-настоящему без потерь
(см. [graceful drain](#graceful-shutdown-и-drain)).

```go
// appCtx НЕ отменяется по SIGTERM — он живёт до конца дренажа.
// Сигналом «пора заканчивать» служит close(w.ch).
func (w *worker) run(appCtx context.Context) {
    batch := make(map[string]int, cfg.BatchSize)
    pending := 0 // число накопленных СОБЫТИЙ (не len(batch)!)
    ticker := time.NewTicker(cfg.FlushInterval)
    defer ticker.Stop()

    flush := func() {
        if pending == 0 {
            return
        }
        if err := w.repo.BatchIncrement(appCtx, batch); err != nil {
            w.metrics.FlushErrors.Inc()
            // лог + при желании retry; батч уже агрегирован
        } else {
            w.metrics.ClicksFlushed.Add(float64(pending))
        }
        w.metrics.BatchSize.Observe(float64(pending)) // события, не len(map)
        batch = make(map[string]int, cfg.BatchSize)
        pending = 0
    }

    for {
        select {
        case ev, ok := <-w.ch:
            if !ok {           // канал закрыт → финальный drain и выход
                flush()
                return
            }
            batch[ev.Code]++
            pending++
            if pending >= cfg.BatchSize {
                flush()
            }
        case <-ticker.C:
            flush()
        }
    }
}
```

> Здесь **намеренно нет** `case <-appCtx.Done()`. Если бы воркер селектил на отмену
> root-контекста, то по SIGTERM (когда `appCtx` уже отменён, но в канале ещё лежат
> тысячи событий) `select` мог бы случайно выбрать ветку отмены и выйти, бросив буфер —
> до `CLICK_CHANNEL_BUFFER` потерянных кликов на **каждом** штатном shutdown. Принудительный
> срыв по таймауту реализуется снаружи, в `main` (см. graceful drain), а не внутри воркера.

## Анализ backpressure

Когда `clickCh` полон, у send есть три стратегии. Send выполняется в обработчике
редиректа — на **горячем пути**, и его поведение определяет латентность.

| Стратегия | Поведение при полном канале | Плюсы | Минусы |
|---|---|---|---|
| **Блокирующий send** (`ch <- ev`) | редирект ждёт, пока освободится место | ни одного потерянного клика | возвращает блокировку на горячий путь — ровно та боль, которую мы убирали; p99 деградирует, риск каскада |
| **Неблокирующий drop** (`select { case ch <- ev: default: drop }`) | клик отбрасывается, инкремент метрики `clicks_dropped_total` | редирект всегда мгновенный, латентность защищена | при перегрузке часть кликов недосчитана |
| **Drop oldest** | вытеснить старое событие | ограниченное отставание | сложнее, тот же риск потери |

### Рекомендация: неблокирующий drop + метрика

Для счётчика кликов **точность не критична**, а **латентность редиректа — это весь смысл
проекта**. Поэтому:

```go
select {
case s.clickCh <- ev:
    // ушло в конвейер
default:
    s.metrics.ClicksDropped.Inc() // backpressure виден в Prometheus
}
```

Обоснование:
- Горячий путь никогда не блокируется — p99 редиректа остаётся плоским даже при перегрузке.
- Дропы **наблюдаемы**: метрика `clicks_dropped_total` показывает, что система на пределе,
  и это сигнал поднять `WORKER_POOL_SIZE`/`CLICK_CHANNEL_BUFFER` или ускорить флаш.
- Буфер `CLICK_CHANNEL_BUFFER` подобран так, чтобы дропы случались только в реальной
  перегрузке, а не в нормальном всплеске.

Точную аналитику (если нужна без потерь) ведёт **Kafka-конвейер** с at-least-once — счётчик
в `links` остаётся «достаточно точным» оперативным числом, а Kafka — источником правды для
расширенной статистики (`/api/v1/links/{code}/stats`).

## Graceful shutdown и drain

Цель: по сигналу (`SIGINT`/`SIGTERM`) дренировать канал **без потерь — при условии, что
дренаж укладывается в `SHUTDOWN_TIMEOUT`**. Это важная оговорка: гарантия не безусловная.
Если за отведённый таймаут воркеры не успели слить остаток (брокер БД лёг, буфер огромный),
часть событий теряется осознанно — таков потолок graceful-периода, который оркестратор
всё равно оборвёт `SIGKILL`. В нормальной работе с разумным буфером дренаж завершается
за миллисекунды и не теряет ничего.

Ключевая идея — **разделить два контекста**:
- `appCtx` (от `signal.NotifyContext`) отменяется по сигналу и служит триггером *начала*
  shutdown, но **не** прокидывается в `select` воркера (см. псевдокод выше);
- воркеры узнают «пора заканчивать» только из **закрытия канала** и дренируют его `range`'ом
  до конца;
- принудительный срыв реализован отдельным таймаутом в `main` через `select` на «воркеры
  закончили» против `time.After(SHUTDOWN_TIMEOUT)` — не внутри воркера.

Последовательность (точный порядок важен):

```
1. Получен SIGINT/SIGTERM (signal.NotifyContext отменяет appCtx).
2. http.Server.Shutdown(ctxTimeout):
     - перестаём принимать новые соединения,
     - даём текущим запросам доиграть (они либо уже сделали send, либо не сделают).
3. close(clickCh):
     - после Shutdown новых send в канал не будет → закрывать безопасно (ровно один раз).
4. Ждём воркеров с потолком по времени:
     done := make(chan struct{}); go func(){ workerPool.Wait(); close(done) }()
     select {
     case <-done:                      // штатно: всё дренировано, потерь нет
     case <-time.After(SHUTDOWN_TIMEOUT): // потолок: выходим, остаток мог не слиться
         metrics.DrainTimeouts.Inc()
     }
5. Останавливаем expiry sweeper (отмена его ctx) и Kafka producer (flush + close).
6. Закрываем pgxpool и redis client.
7. Выходим.
```

ASCII последовательности:

```
signal ─▶ http.Shutdown ─▶ close(clickCh) ─▶ workers range-drain & final flush ─▶ wg.Wait
                                                     │                               │
                                                     │              select{ done | timeout }
                                              sweeper.Stop / kafka.Close
                                                     │
                                              pgxpool.Close / redis.Close ──▶ exit
```

Две обязательные тонкости порядка:

1. **Нельзя** `close(clickCh)` до `http.Shutdown` — иначе обработчик может сделать send в
   закрытый канал и упасть с паникой. Сначала перестаём принимать запросы, потом закрываем
   канал. Канал закрывает **один** владелец (тот, кто его создал), ровно один раз.
2. Воркер **не** селектит на `appCtx.Done()`. Нормальный и единственный путь завершения
   воркера — увидеть `ok == false` на закрытом канале и выйти после финального `flush()`.
   Иначе (как разобрано в псевдокоде) по SIGTERM `appCtx` уже отменён, и `select` мог бы
   выбрать ветку отмены, бросив непрочитанный буфер. Срыв по таймауту — снаружи, в `main`.

## Kafka: семантика доставки

Каждое событие клика дополнительно уходит в топик `clicks` для внешней аналитики.
README просит «потрогать оба» подхода — fire-and-forget и at-least-once.

### Форма события и ключ партиции

```go
type ClickEvent struct {
    ClickID string    // уникальный id события (UUID или code:unixnano:rand)
    Code    string    // он же ключ партиции Kafka
    TS      time.Time
}
```

Две вещи, критичные для корректной аналитики и часто упускаемые:

- **Ключ записи Kafka = `Code`.** Тогда все события одного кода попадают в одну партицию:
  consumer-группа не размазывает их по воркерам, а порядок и дедуп в пределах кода
  предсказуемы. Без ключа (round-robin) аддитивный upsert по счётчику всё равно сойдётся
  (он коммутативен), но любая дедупликация и пер-ключевая логика усложняются.
- **`ClickID` обязателен для at-least-once.** Аддитивный upsert `clicks = clicks + 1`
  (см. [data-model.md](./data-model.md#этап-2-click_events-и-агрегаты)) **не** идемпотентен
  против повторной доставки: at-least-once по определению может прислать дубль, и без
  дедупа он удвоит счётчик в `click_aggregates`. Поэтому либо consumer дедупит по `ClickID`
  (таблица/множество уже виденных id), либо честно признаём, что аналитика
  **приблизительная**, и не называем её «точной».

### Fire-and-forget (asynchronous, acks=1 / acks=0)

```go
// franz-go / segmentio: отправляем и не ждём подтверждения в горячем коде
producer.Produce(ctx, record, func(r *kgo.Record, err error) {
    if err != nil {
        metrics.KafkaProduceErrors.Inc() // только метрика, не блокируем
    }
})
```

| | |
|---|---|
| Латентность | минимальная, не на горячем пути |
| Гарантии | можно потерять событие при сбое брокера/буфера |
| Когда | счётчик кликов, оперативная аналитика «примерно» — допустима потеря |

### At-least-once (acks=all, idempotent producer, ретраи)

```go
cfg := []kgo.Opt{
    kgo.RequiredAcks(kgo.AllISRAcks()),
    kgo.ProducerBatchMaxBytes(...),
    kgo.RecordRetries(math.MaxInt), // ретраим до успеха
}
```

| | |
|---|---|
| Латентность | выше (ждём подтверждение всех реплик) |
| Гарантии | событие не теряется; возможны дубли → дедуп по `ClickID` обязателен, иначе двойной счёт |
| Когда | если `/{code}/stats` должна быть точной; consumer дедупит по `ClickID` перед upsert'ом |

> Без дедупа at-least-once даёт **не** «точную», а «без потерь, но с возможными дублями»
> аналитику. «Точная» = at-least-once **плюс** дедуп по `ClickID` (приближение к
> effectively-once на стороне consumer). Это разные гарантии — не путать.

### Рекомендация

- **Счётчик в `links`** — питается из внутреннего канала с drop-политикой (быстро, «достаточно точно»).
- **Kafka-поток** — пускаем как отдельную ветку. Для учебной демонстрации: начать с
  **fire-and-forget** (проще, видно неблокирующую отправку), затем переключить конфигом на
  **at-least-once** и обсудить дубли/идемпотентность consumer'а. Producer тоже не должен
  сидеть на горячем пути: отправку делает либо воркер-пул, либо отдельная горутина, читающая
  из того же события.

> Решение (Kafka vs NATS): **Kafka**. Учебная ценность выше — партиции, consumer-группы,
> acks, idempotent producer, ретраи — это то, что спрашивают на собесах. NATS проще, но
> меньше «поверхности» для отработки семантики доставки. Если важна лёгкость локального
> поднятия — NATS JetStream допустимая замена, API конвейера от этого не меняется.

## Решение 301 vs 302

| | 301 Moved Permanently | 302 Found |
|---|---|---|
| Кэширование браузером | агрессивно кэшируется, часто навсегда | не кэшируется по умолчанию |
| Влияние на счётчик | браузер ходит мимо нас → **клики недосчитываются** | каждый клик доходит до сервиса |

**Рекомендация: 302 Found.** Для сокращателя со статистикой кликов 301 ломает учёт —
браузер закэширует и перестанет обращаться к сервису, счётчик замрёт. 302 гарантирует, что
каждый клик проходит через наш редирект и попадает в конвейер. Минус — нет браузерного кэша,
но для учебного сервиса с акцентом на метрики кликов это правильный компромисс.

Почему не 307/308: пары 307/308 отличаются от 302/301 только тем, что **сохраняют HTTP-метод**
(не позволяют редиректу превратить POST в GET). Редирект сокращателя — всегда `GET /{code}`,
другого метода тут не бывает, поэтому сохранение метода нерелевантно, и выбор сводится
исключительно к семантике кэширования: temporary (302) против permanent (301). Это, кстати,
и есть чистейшее обоснование именно 302 — нам важна не перманентность, а то, что каждый
переход доходит до сервиса.

> Эта же «временность» 302 — техническая предпосылка для будущего перенацеливания ссылки
> (сменить `original_url`, не меняя `code`). Сейчас отдельного endpoint для этого нет
> (см. [api.md](./api.md)) — это явный нереализованный пункт, а не возможность «из коробки».
